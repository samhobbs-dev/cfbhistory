import axios from "axios";

const GameService = {
    // API calls
    getTeamGamesForYear(year: number/*,team: Team*/) {}
    ,
    // Group calls
    populateAllTeamGamesForYear(year: number/*,teams[]*/) {}
}

export default GameService;